# Staking Reliability

This placeholder documents Starkzap staking reliability checks.

Planned contents:
- Pool discovery and validator preset verification.
- Timeout/abort behavior during staking queries.
- Membership-sensitive flows (`enter`, `add`, `exit intent`, `exit`).
- Recovery actions for timeout and revert scenarios.

