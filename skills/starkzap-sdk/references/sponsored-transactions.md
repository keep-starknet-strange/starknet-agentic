# Sponsored Transactions

This placeholder documents sponsored transaction behavior in Starkzap.

Planned contents:
- `feeMode: "sponsored"` preconditions and paymaster assumptions.
- Deployment readiness checks vs execution checks.
- Retry/backoff guidance for RPC and paymaster timeouts.
- Validation checklist for `wallet.preflight(...)` and `wallet.execute(...)`.

