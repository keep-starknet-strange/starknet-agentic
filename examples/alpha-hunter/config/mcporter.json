{
  "mcpServers": {
    "tokenterminal": {
      "command": "npx",
      "args": [
        "-y",
        "@tokenterminal/mcp-server@latest"
      ],
      "env": {
        "TT_API_KEY": "${TOKEN_TERMINAL_API_KEY}"
      }
    }
  },
  "imports": []
}