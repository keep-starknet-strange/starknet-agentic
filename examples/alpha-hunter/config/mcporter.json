{
  "mcpServers": {
    "tokenterminal": {
      "command": "npx",
      "args": [
        "-y",
        "@tokenterminal/mcp-server"
      ],
      "env": {
        "TT_API_KEY": "${TOKEN_TERMINAL_API_KEY}"
      }
    }
  },
  "imports": []
}